// @flow

import { defineMessages } from 'react-intl';

export default defineMessages({
  button: {
    id: 'containers.Template.button',
  },
  buttonAsync: {
    id: 'containers.Template.buttonAsync',
  },
});
